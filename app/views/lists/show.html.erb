<h2><%= @list.name %> </h2>

<% @list.tasks.each do |task| %>
<%= form_for(task) do |f| %>
    <%= f.label :name do %>
      <%= f.check_box :completed %>
      <%= link_to task.name, task_path(task) %>
    <% end %>
<% end %>
<% end %>
