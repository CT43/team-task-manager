
<h2><%= @team.name %></h2>
<% @team.users.each do |user| %>

  <%= user.name %> ---
  <% user.lists.each do|list|  %>
    <%= link_to list.name, user_list_path(current_user, list) %>
    <% end %>
    <br>
    <br>
<% end %>
